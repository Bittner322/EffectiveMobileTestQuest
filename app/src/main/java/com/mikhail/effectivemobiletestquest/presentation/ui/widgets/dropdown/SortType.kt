package com.mikhail.effectivemobiletestquest.presentation.ui.widgets.dropdown

import com.mikhail.effectivemobiletestquest.R

enum class SortType(val sortType: Int) {
    BY_POPULARITY(R.string.catalog_sort_by_popular),
    BY_PRICE_UP(R.string.catalog_sort_by_price_up),
    BY_PRICE_DOWN(R.string.catalog_sort_by_price_down)
}